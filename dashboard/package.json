{
  "name": "ai-traffic-control-dashboard",
  "private": true,
  "type": "module",
  "scripts": {
    "test:unit": "node --test tests/unit/*.test.mjs",
    "test:e2e": "playwright test tests/e2e/ --reporter=line --workers=4",
    "test": "npm run test:unit && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "1.53.0"
  }
}
